defaultNamespace: lingolearn

helm:
  takeOwnership: true

imageScans:
  - image: ghcr.io/willuhmjs/lingolearn
    tagName: lingolearn-image
    policy:
      semver:
        range: ">=0.0.0"
    interval: 5m0s
